// ;(function (window) {
// 	"use strict";

// 	var app = window.app || {};

// 	app.plugin = {
//         getUrl: function (url) {
// 			return chrome.extension.getURL(url);
// 		}
// 	};

// })(window);